{
  "name": "codemap",
  "version": "0.1.0",
  "private": true,
  "description": "A living knowledge graph of your codebase. Agents read it as memory over MCP, humans watch it as live telemetry, and both use it as a prompting surface.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "index": "node packages/indexer/dist/cli.js",
    "dev:server": "node packages/server/dist/index.js",
    "dev:web": "npm run dev -w @codemap/web"
  },
  "engines": {
    "node": ">=20"
  }
}
